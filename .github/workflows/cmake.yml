name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: make
      run: cmake -B ./build -D CMAKE_BUILD_TYPE=Release . && cmake --build ./build --config Release && cmake --install ./build --config Release
